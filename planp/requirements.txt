Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Migrate>=4.0
Flask-Login>=0.6
psycopg2-binary>=2.9
gunicorn>=21.2
python-dotenv>=1.0
bleach>=6.1
pytest>=8.0
pytest-cov>=4.1
requests>=2.31
Flask-Limiter>=3.5
Flask-Cors>=4.0
openpyxl>=3.1

# ADR D5 — fast-layer FHIR R5 validator (pydantic models).
# Used in tests to validate response shape; canonical conformance still
# requires the HL7 Java validator_cli.jar via `make conformance` (TBD).
fhir.resources>=8.0
