Current focus
=============

epic #516 — Guided PlanDef authoring assistant (opt-in, Claude-assisted).

DONE (2026-07-31, local build, all tests green — 280 passed):
  - GA-1 #517  app/services/plandef_validation.py   (deterministic Layer-1 floor)
  - GA-2 #518  app/services/plandef_assistant.py     (Claude Layer-2, selectable model)
  - GA-3 #519  app/api/authoring.py + config flag     (opt-in /api/v1/authoring, default OFF)
  - GA-6 #522  docs/plandef_authoring_assistant_design.md (+ .docx)

The tool is OPT-IN and does NOT change existing concept/plandef save behaviour.
Enable with AUTHORING_ASSISTANT_ENABLED=true; set ANTHROPIC_API_KEY to turn on
the Claude assistant (Layer 2). Without a key, Layer-1 /authoring/validate still
works.

Next-up
-------
1. GA-4 #520 — builder UI: wire a non-blocking Check/Assist panel into
   templates/plandefinitions/builder.html (+ concept form). Model picker from
   GET /api/v1/authoring/models; hidden when disabled/no key. Purely additive.
2. GA-5 #521 — OPERATOR SIGN-OFF: promote validate_concept/validate_plandef to
   fail-closed on the write paths (web + API) so ERROR-severity rejects the save.
   Requires a data audit of existing rows that would newly fail + an audited
   per-field override escape hatch. Do NOT deploy without go-ahead.
3. Server deploy (when authorised): scp the new files + config; no migration.
   Set AUTHORING_ASSISTANT_ENABLED + ANTHROPIC_API_KEY in server .env; restart
   own gunicorn. Verify GET /api/v1/authoring/models behind SSO.

Prior focus (still valid): §6 FHIR R5 terminology profile COMPLETE (2026-06-22).
