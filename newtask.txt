Current focus
=============

#530 — Transfer page (plan.pdhc): promote synthetic cdr_6 content into a
working CDR. BUILT local 2026-08-06, full suite 320 passed. Thin proxy to
cdr_6's server-side engine (#529, DEPLOYED). plan.pdhc is authoring-plane and
does NOT move data itself — it only triggers cdr_6.

  - GET /transfer (SSO page) + GET/POST /api/v1/transfer/{targets,preview,execute}
    (@requires_role read_write). Source fixed to cdr_6 (synthetic) -> safe for
    any authenticated professional. Calls cdr_6 as X-Source-Service: sim.pdhc
    (cdr_6 sim-only, Rule 2) with CDR6_SERVICE_KEY = cdr_6's SIM_PDHC_SERVICE_KEY.

Next-up
-------
1. Deploy (operator go-ahead): docker compose up -d --build (pdhc_app). Set in
   the server .env: CDR6_BASE_URL=http://host.docker.internal:9055 and
   CDR6_SERVICE_KEY = cdr_6's SIM_PDHC_SERVICE_KEY. Also set the destination
   URLs on the cdr_6 side (#529): CDRn_BASE_URL=http://host.docker.internal:<port>
   (cdr1 9046, cdr2 9146, cdr3 9246, cdr4 9346, cdr5 9446) in cdr_6's .env so a
   real transfer can reach cdr1-5.
2. Live e2e smoke: sim generate-from-plandef ... --target cdr_6 --run-id
   move-smoke ; then on the Transfer page pick cdr2 + run move-smoke -> Preview
   (count == generated) -> Execute -> verify cdr2 rises; re-run with purge and
   confirm the run left cdr_6 /api/v1/runs. Purge the cdr2 test run afterwards.
3. No migration either side.
